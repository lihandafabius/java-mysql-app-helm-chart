apiVersion: v1
kind: Service
metadata:
  name: {{ include "java-mysql-app.fullname" . }}
  labels:
    {{- include "java-mysql-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "java-mysql-app.selectorLabels" . | nindent 4 }}
